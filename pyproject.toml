[tool.uv.workspace]
members = ["apps/api", "apps/worker", "packages/sdk-py"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-ra --strict-markers --strict-config"
